# Benchmark results

See `results.csv` for machine-readable results.

Main 768-atom total energy for all completed rank counts:

```text
-799.0006378657 H
```

Best observed runtime: 15.30 s using 4 MPI ranks on compute01.

The 8-rank/two-node run completed but required 169.61 s, demonstrating communication overhead in the VMware network for this workload.
